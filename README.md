# workspace-portal-hello-world

Disposable test repo for the Workspace Portal Deployments module.

Three branches: develop, staging, main — each renders different content for visibly distinguishing which environment is serving a request.

DO NOT use for production traffic. This repo exists only to validate the deploy pipeline.
